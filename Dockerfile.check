# Ephemeral verification image. Source is bind-mounted and dependencies live in
# named Docker volumes, so Node.js and package installations never touch host.
FROM node:22-bookworm-slim

RUN corepack enable && corepack prepare pnpm@10.15.0 --activate

ENV PNPM_STORE_DIR=/pnpm/store

WORKDIR /workspace
